# -*- coding: utf-8 -*-
"""
aas.attendance — Attendance lifecycle package for Single Shot AAS v2.5.

Submodules:
    spreadsheet   - Google Sheets integration (v2.0, preserved)
    state_machine - Session state machine and transitions (v2.5)
    session       - AttendanceSession domain object and service (v2.5)
    records       - AttendanceRecord domain object and builders (v2.5)
    overrides     - L1/L2/L3 manual fallback correction services (v2.5)
    repository    - Durable SQLite finalization transaction (v2.5)
"""
