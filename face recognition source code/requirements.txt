# ============================================================
#  Single Shot AAS — Requirements v2
#  Cross-platform (Linux / Windows / macOS / Raspberry Pi)
# ============================================================
#
# Linux pre-requisites (run once):
#   sudo apt install cmake libboost-all-dev portaudio19-dev espeak
#
# Install all packages:
#   pip install -r requirements.txt
# ============================================================

# --- Core Face Recognition ---
face-recognition==1.3.0
face-recognition-models==0.3.0
dlib==19.24.2
opencv-python==4.9.0.80
numpy>=1.24.0,<2.0
Pillow>=10.0.0

# --- Google Drive + Sheets Integration (OAuth2) ---
gspread==6.0.2
google-auth==2.29.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2==0.2.0
google-api-python-client>=2.100.0

# --- Web Dashboard ---
Flask==3.0.3
flask-cors==4.0.0
Flask-Session>=0.6.0

# --- Voice Command Trigger (offline) ---
SpeechRecognition==3.10.4
sounddevice==0.4.7
vosk==0.3.45

# --- Text-to-Speech Announcement (offline) ---
pyttsx3>=2.90

# --- Email ---
secure-smtplib==0.1.1

# --- Utilities ---
python-dotenv==1.0.1
